<h1><strong>Say hello to all these cuties!</strong></h1>

<%= link_to "Add a pup", new_dog_path(@dog), class:"btn btn-success" %>

<ul>
  <% @dogs.each do |dog| %>
    <li>
      <%= link_to dog.name, dog_path(dog)%>
     
</ul>
<% end %>
